<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car List</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <!-- Add Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Add Bootstrap Multi-Select Dropdown CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-multiselect/0.9.16/css/bootstrap-multiselect.css" rel="stylesheet">
    <!-- Add jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <!-- Add Bootstrap Multi-Select Dropdown JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-multiselect/0.9.16/js/bootstrap-multiselect.min.js"></script>
    <script src="multiselect-dropdown.js" ></script>
</head>
<body>
    <div class="form-group col-sm-8">
        <label for="multiselectSearch">Search</label>
        <select id="multiselectSearch" multiple class="form-control">            
            <option value="brand1">Brand 1</option>
            <option value="brand2">Brand 2</option>
            <option value="brand2">Brand 3</option>            
        </select>
    </div>

    <div class="container mt-5">
        <h1>Search Vehicles</h1>
        <form id="searchForm" class="form-inline mb-4">
            <div class="form-group mx-sm-3 mb-2">
                <input type="text" class="form-control" id="searchQuery" placeholder="Enter vehicle details">
            </div>
            <button type="submit" class="btn btn-primary mb-2">Search</button>
        </form>
        
        <h2>Car List</h2>        
        <ul>
            {{#each items}}
            <li>
                <strong>ID:</strong> {{this.id}} <br>
                <strong>Brand:</strong> {{this.brand}} <br>
                <strong>Model:</strong> {{this.model}} <br>
                <strong>Price:</strong> {{this.price}} <br>            
            </li>
            {{/each}}
        </ul>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="/js/searchHandler.js"></script>
</body>
</html>
